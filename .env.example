# Server port
PORT=8080

# Database file path (SQLite)
DB_PATH=data.db

# JWT signing secret (change in production!)
JWT_SECRET=change-me-in-production

# Allowed CORS origin (use * for development, set specific URL in production)
ALLOWED_ORIGIN=*
